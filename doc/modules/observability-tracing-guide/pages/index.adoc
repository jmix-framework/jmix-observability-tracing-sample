:sample-project: jmix-observability-tracing-sample

= Observability: Distributed Tracing

In this guide, you will learn how to implement distributed tracing for your Jmix applications using Micrometer and OpenTelemetry to track requests across multiple services and gain complete visibility into your application's behavior.

[[requirements]]
== Requirements

To effectively use this guide, you will need the following:

. xref:ROOT:setup.adoc[Setup] the development environment.

. Complete the xref:observability-logging-guide:index.adoc[Centralized Logging Guide] to understand the foundational observability concepts and have the required infrastructure components running.

. Clone the https://github.com/jmix-framework/{sample-project}[sample project^] and switch to `{page-origin-branch}` branch:
+
[source,subs=attributes+]
----
git clone https://github.com/jmix-framework/{sample-project}
cd {sample-project}
git checkout {page-origin-branch}
----

[[what-we-are-going-to-build]]
== What We are Going to Build

This guide focuses on enhancing the Petclinic Backend and Portal applications from the centralized logging guide with comprehensive distributed tracing capabilities.

Building upon the existing OpenTelemetry infrastructure, we will demonstrate how to track requests as they flow from the Portal frontend through the Backend to the database, providing complete visibility into distributed request execution.

The final application includes:

* Automatic Instrumentation: Zero-code tracing for HTTP requests, REST DataStore calls that works immediately after configuration
* Custom Business Tracing: Manual spans for complex operations like owner registration, enriched with meaningful business context
* Grafana Tempo Integration: Complete trace visualization and analysis alongside existing logs in a unified observability interface
* Cross-Service Correlation: Track individual requests from Portal frontend through Backend to database with precise timing and error information

[[introduction-distributed-tracing]]
== Introduction to Distributed Tracing

Before diving into the technical setup, let's look at what distributed tracing is and why it becomes essential when working with multiple applications.

In the previous xref:observability-logging-guide:index.adoc[Centralized Logging Guide], we established the foundation for observability by bringing logs from multiple services into a single interface. Centralized logging was our first step toward understanding distributed systems - instead of searching through separate log files on different servers, we could view all logs in Grafana and even correlate them temporally to understand what was happening across services at a specific moment in time.

However, as systems grow and handle more concurrent requests, centralized logging alone reaches its limits. While we can see logs from different services in one place and correlate them by timestamp, this temporal correlation becomes increasingly difficult with higher load. When dozens or hundreds of requests are processed simultaneously, determining which log entries belong to the same user request becomes nearly impossible. Furthermore, centralized logging lacks causal correlation - there's no automatic way to understand the cause-and-effect relationships between events across services.

Distributed tracing addresses these limitations by adding a crucial capability: the ability to track a single request as it travels through multiple services, creating a complete picture of how different parts of your system interact to fulfill that request. While centralized logging gives us visibility into what's happening across services, distributed tracing adds the context of *why* and *how* these events are related.

Consider what happens when a pet owner logs into the Petclinic Portal and views their list of pets. This seemingly simple action actually involves multiple steps across different applications:

1. The Portal receives the user's request to view pets
2. Portal makes a REST API call to the Backend using the REST DataStore
3. Backend processes the request and queries the database
4. Database returns the pet data
5. Backend serializes the response back to Portal
6. Portal renders the pet list for the user

With traditional logging, each application would record its own activities independently. You might see a log entry in the Portal about loading pets, and separately, a log entry in the Backend about processing a pet query. However, there's no automatic way to connect these individual log entries into a single, cohesive story of what happened during that specific user request.

Distributed tracing solves this by creating a *trace* - a collection of related *spans* that represent the work done by each service for a single request. Each span contains timing information, operation details, and a unique identifier that links it to other spans in the same trace.

Think of it this way: logs are like individual photographs that capture moments in time across different locations, while traces are like a video that shows the complete journey of a request through your system, revealing the connections between those moments.

By building distributed tracing on top of our centralized logging foundation, we gain several key benefits:

* Request Flow Visibility: See exactly how a request moves through your system and where time is spent
* Performance Analysis: Identify bottlenecks by examining which operations take the longest
* Error Correlation: When something goes wrong, trace the exact path that led to the error
* Service Dependencies: Understand how your services interact and depend on each other
* Causal Relationships: Unlike temporal correlation in centralized logging, traces reveal the actual cause-and-effect chain between events

In distributed systems, this enhanced visibility becomes critical for understanding system behavior, diagnosing issues, and optimizing performance across service boundaries. Distributed tracing complements centralized logging by adding the missing piece: automatic correlation of related events across services, even under high load with many parallel requests.

[[spring-boot-actuator-and-micrometer]]
== Spring Boot Actuator and Micrometer Tracing

Before configuring the specific tracing dependencies, it's important to understand the foundation that Jmix and Spring Boot already provide for observability.

As described in the xref:observability-logging-guide:index.adoc#spring-boot-actuator[Centralized Logging Guide], Spring Boot Actuator serves as the main way to expose operational data from applications. While Actuator itself is not included by default in Jmix applications, the underlying Micrometer dependencies are already present through Spring Boot.

With Spring Boot 3, Micrometer has evolved beyond just metrics collection to provide a comprehensive observability facade that includes:

* Micrometer Observation API: A unified API for instrumenting code once and getting both metrics and traces
* Micrometer Tracing: A vendor-neutral tracing facade that works with different implementations (OpenTelemetry, Brave/Zipkin, AWS X-Ray)

This means Jmix applications already have the foundation for distributed tracing built in through these Micrometer dependencies. When we add `spring-boot-starter-actuator` and `micrometer-tracing-bridge-otel` in the next sections, we're:

1. Enabling Actuator to expose the observability features
2. Telling Micrometer to use OpenTelemetry as its tracing implementation

The bridge translates Micrometer's observations into OpenTelemetry spans, providing automatic instrumentation for HTTP requests and other operations without requiring any code changes.

[NOTE]
====
Because Jmix includes Micrometer through Spring Boot, the foundation for distributed tracing is already present. This makes enabling tracing primarily a matter of adding Actuator and configuring which tracing backend to use.
====

[[setting-up-distributed-tracing]]
== Setting Up Distributed Tracing

Now that we understand what distributed tracing provides and the foundation that Jmix already includes, let's enhance our existing observability infrastructure to support trace collection and visualization.

We will build upon the OpenTelemetry Collector and Grafana setup from the centralized logging guide, adding Grafana Tempo as our trace storage backend and configuring our applications to export traces.

[[adding-tempo]]
=== Adding Grafana Tempo

https://grafana.com/oss/tempo/[Tempo^] is Grafana's distributed tracing backend that stores and queries traces efficiently. Like Loki for logs, Tempo integrates seamlessly with Grafana for visualization and correlates with other observability data.

We extend our existing `docker-compose.yaml` from the logging guide to include Tempo:

.docker-compose.yaml - Tempo service
[source,yaml,indent=0]
----
include::example$/docker/docker-compose.yaml[tags=tempo]
----

Tempo uses a simple configuration that stores traces locally and makes them available for querying through Grafana.

.tempo-config.yaml
[source,yaml,indent=0]
----
include::example$/docker/tempo-config.yaml[]
----

[[collector-configuration]]
=== Updating OpenTelemetry Collector

Next, we need to update our OpenTelemetry Collector configuration to receive traces from applications and forward them to Tempo. The collector already handles logs from the previous guide - now we extend it to process traces as well.

.otelcol-config.yaml - Complete configuration with trace support
[source,yaml,indent=0]
----
include::example$/docker/otelcol-config.yaml[tags=receivers-with-traces;processors;exporters-with-traces;service-with-traces]
----

The key additions for tracing are:

* OTLP receiver on port `4318` for trace data.
* OTLP exporter that forwards traces to Tempo running on port `3200`.
* Updated service pipeline that routes traces from receivers through processors to the Tempo exporter.

[[grafana-data-source]]
=== Configuring Grafana Data Sources

We need to configure Grafana with both Loki and Tempo as data sources, and enable the bidirectional integration between them for seamless navigation between logs and traces.

.grafana-datasources.yaml - Complete configuration with bidirectional log-trace correlation
[source,yaml,indent=0]
----
include::example$/docker/grafana-datasources.yaml[tags=start;loki-with-traces;tempo-with-logs]
----

This configuration provides:

* Loki with trace correlation: Extracts trace IDs from structured logs using a regex pattern and creates clickable links to view the corresponding trace in Tempo
* Tempo with log correlation: Enables navigation from traces back to related logs in Loki, filtered by trace ID and service name

The key integration features:

* Derived fields in Loki: The `matcherRegex` pattern `"traceid":"(\w+)"` extracts trace IDs from JSON-formatted logs and creates clickable links
* Traces to logs in Tempo: Maps the `service.name` from traces to the `job` label in Loki for proper filtering
* Bidirectional navigation: Users can jump from a log entry to its trace, or from a trace span to all related logs

With this setup, Grafana becomes a unified interface where logs and traces are seamlessly connected, making it easy to investigate issues by moving between different telemetry types.

[[enabling-tracing-backend]]
=== Enabling Tracing in Petclinic Backend

Now we configure the Petclinic Backend application to export traces to our OpenTelemetry Collector. This requires adding tracing dependencies and configuring the OTLP exporter.

First, add the required dependencies to the Backend's `build.gradle`:

.build.gradle - Tracing dependencies
[source,groovy,indent=0]
----
include::example$petclinic/build.gradle[tags=tracing-dependencies]
----

These dependencies provide:

* **micrometer-tracing-bridge-otel**: Bridges Spring Boot's Micrometer Tracing API to the OpenTelemetry protocol, enabling automatic instrumentation of HTTP requests and other operations
* **opentelemetry-exporter-otlp**: Exports trace data using the OTLP protocol to our OpenTelemetry Collector

[NOTE]
====
The `spring-boot-starter-actuator` dependency, which includes Micrometer core and observation capabilities, is already present in the dependencies section above the tracing configuration.
====

Next, configure the tracing export settings in `application.properties`:

.application.properties - Tracing configuration
[source,properties,indent=0]
----
include::example$petclinic/src/main/resources/application.properties[tags=tracing-config]
----

Key configuration elements:

* **Sampling probability**: Set to `1.0` (100%) for development environments to capture all traces. In production, you might reduce this to `0.1` (10%) to balance visibility with performance
* **OTLP tracing export**: Enables trace export via HTTP to the OpenTelemetry Collector


[NOTE]
====
We set the value `management.otlp.tracing.endpoint` is used for local development. In the docker environment we will override this value to point to the HTTP address of the OTEL Collector.
====

[[enabling-tracing-portal]]
=== Enabling Tracing in Petclinic Portal

The Portal application requires the same tracing configuration to participate in distributed traces and track requests from the frontend perspective.

Add the tracing dependencies to the Portal's `build.gradle`:

.build.gradle - Tracing dependencies
[source,groovy,indent=0]
----
include::example$portal/build.gradle[tags=tracing-dependencies]
----

Configure the Portal's tracing export settings in `application.properties`:

.application.properties - Tracing configuration
[source,properties,indent=0]
----
include::example$portal/src/main/resources/application.properties[tags=tracing-config]
----

[[docker-environment-configuration]]
=== Configuring the Docker Environment

When running the applications in Docker containers, the tracing endpoints need to be configured to point to the internal Docker network addresses. The `docker-compose.yaml` file includes environment variables that override the local development settings:

.docker-compose.yaml - Environment variables for tracing
[source,yaml,indent=0]
----
environment:
  # ... other environment variables ...
  # Logging export
  MANAGEMENT_OTLP_LOGGING_ENDPOINT: http://otel-collector:4318/v1/logs
  # Tracing export
  MANAGEMENT_OTLP_TRACING_ENDPOINT: http://otel-collector:4318/v1/traces
----

These environment variables override the `application.properties` settings when running in Docker, ensuring that the applications can reach the OpenTelemetry Collector using the internal Docker network hostname (`otel-collector`) instead of `localhost`.

[[verifying-setup]]
=== Verifying the Tracing Setup

With all components configured, let's verify that our distributed tracing setup works correctly.

. Start the complete infrastructure including the new Tempo service:
+
[source,bash]
----
docker-compose -f docker/docker-compose.yaml up -d
----

. Start both the Petclinic Backend and Portal applications (either locally or via Docker)

. Open Grafana at http://localhost:3000 (username: `admin`, password: `admin`)

. Navigate to **Configuration > Data sources** and verify that both Loki and Tempo appear as configured data sources

. In Grafana, go to **Explore** and select **Tempo** from the data source dropdown

If the setup is working correctly, you should see Tempo available as a data source. The applications will begin sending traces to the Collector, which forwards them to Tempo for storage and querying.

In the next section, we'll explore how distributed tracing immediately provides valuable insights without any code changes, using a simple visit list viewing scenario.

[[distributed-tracing-in-action]]
== Distributed Tracing in Action

With all the infrastructure and configuration in place, let's see distributed tracing work across our services. We'll use a simple scenario: viewing the list of visits in the Petclinic Portal, which triggers a REST API call to the Backend.

[[viewing-visits-portal]]
=== Viewing Visits in the Portal

When a pet owner logs into the Petclinic Portal and navigates to the visits list, the Portal application needs to fetch the visit data from the Backend through the REST DataStore.

.Petclinic Portal - Visit List View
image::portal-visit-list-view.png[Petclinic Portal Visit List View, link="_images/portal-visit-list-view.png"]

This user action triggers a chain of operations across both services:

1. The Portal receives the request and creates a new trace with a unique ID
2. The REST DataStore calls the Backend API, including the trace ID in HTTP headers (`traceparent`)
3. The Backend extracts the trace context from headers and creates child spans for its operations  
4. Both services export their spans to the OpenTelemetry Collector, which forwards them to Tempo

Without trace propagation through HTTP headers, each service would generate isolated traces with no connection between them.

[[logs-with-trace-context]]
=== Logs with Trace Context

In Grafana's Drilldown > Logs view, filter for logs from the `jmix-petclinic-portal` service to see what happens when loading visits:

.Grafana Logs - Portal loading visits
image::grafana-logs-with-trace-ids.png[Grafana Logs showing Portal loading visits, link="_images/grafana-logs-with-trace-ids.png]

The first log entry in the Table "Loading visits for owner" now includes the `trace_id` as the unique identifier for the interaction. You can already see that the second log message that comes from the UI Controller contains the same `trace_id` value as it originated from the same UI interaction. This shows, that the trace identifier is used across multiple parts in the application that are triggered by one UI interaction.

But how can we now see also logs from multiple services that belong to the same trace? This we will see in the next section.

[[from-logs-to-traces]]
=== From Logs to Traces

Clicking the trace link opens the complete distributed trace in Tempo:

.Grafana Tempo - Distributed Trace View
image::grafana-trace-details.png[Tempo showing the complete distributed trace, link="_images/grafana-trace-details.png"]

The trace view shows the complete request flow across both services. It includes the `jmix-petclinic-portal` as well as the `jmix-petclinic` as the backend.

The different entries in the tree structure on the left represent the different spans that are part of the trace. The span with the name `http post` represents the HTTP Client interaction on the portal side. Additional information (span attributes), like the requested URL, the HTTP method etc. are stored there and can be viewed by clicking on the span name.

Additionally, each span displays timing information: its start time relative to trace beginning, duration, and whether operations run sequentially or in parallel. The trace confirms that the Portal's request triggered Backend processing, with both services participating in the same distributed trace.

[[understanding-trace-details]]
=== Understanding the Trace Details

Spring Boot with Micrometer creates spans for all major operations without requiring tracing code in business logic. This includes incoming HTTP requests to both Portal and Backend, outgoing REST API calls from Portal to Backend.

Each span in the trace provides timing information that helps identify performance bottlenecks. When errors occur, the trace shows exactly where in the request flow they happened. The visual representation makes service dependencies clear, and the timing breakdown separates network latency from actual processing time.

[NOTE]
====
You can enhance traces with custom spans for business-specific operations, covered in the next section.
====